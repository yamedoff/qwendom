## Society Doc Plan

1. Add a new document under `docs/` focused on making the Qwendom society useful through Agno-native primitives.
2. Cover the current runtime limitations and the target operating model centered on tool access, artifact generation, routing, validation, memory, and knowledge.
3. Map the proposed model onto the current codebase file-by-file so implementation work is actionable.
4. Run at least two external review passes with Opencode `opencode-go/qwen3.7-max` and fix any concrete gaps they identify.
