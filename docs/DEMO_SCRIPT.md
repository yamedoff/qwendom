# Three-minute Qwendom demo script

Record one uninterrupted walkthrough of the persisted successful Qwen run
`f743c4f6-2d86-4daf-922e-ef2a13210fa7` (175 durable events,
`complete_with_warnings`). Do not imply that replay is a new live execution.
Do not expose keys or personal data, or claim deployment/isolation that is not
present. The existing `dogfood-output/.../fresh-run.webm` is bug-reproduction
evidence and must not be submitted as the final demo.

| Time | Screen and narration |
|---|---|
| 0:00–0:15 | Intake. “One opaque answer cannot show who researched, challenged, or verified it. Qwendom makes that work accountable.” |
| 0:15–0:35 | Select the persisted reference task and state that this is a durable replay of a real direct-Qwen run. Show the four core roles, capability-based assignments, and non-voting specialist boundary. |
| 0:35–1:10 | Live-history view. Show Researcher tool intent/provenance, Builder output, Critic challenge, and retry evidence if naturally present. Do not stage fake events or claim the replay is executing now. |
| 1:10–1:40 | Show proposals, disagreement, ballots, tally, and preserved dissent or typed user escalation. |
| 1:40–2:10 | Review/Recap/Dossier. Highlight required acceptance evidence and the truthful terminal state. |
| 2:10–2:35 | Open the frozen v3 report. “Both modes scored 100. Society averaged 233 seconds and 810,389 total tokens; single agent averaged 11.71 seconds and used 8,310 total tokens. This proves inspectable teamwork, not efficiency.” |
| 2:35–2:52 | Show `/health` model preflight and the architecture diagram. State that the run uses direct Qwen Cloud/DashScope. |
| 2:52–3:00 | Show repository URL and Track 3 name. |

Before recording:

1. Use `LLM_PROVIDER=qwen` and `QWEN_MODEL=qwen3.7-plus`.
2. Confirm `/health` reports a ready direct-Qwen configuration.
3. Confirm task `f743c4f6-2d86-4daf-922e-ef2a13210fa7` still loads all 175 events across Live, Review, Recap, and Dossier.
4. Keep the capture below three minutes and use no unlicensed music.
5. Upload publicly, then verify the URL signed out.
6. Confirm the report is frozen v3, shows 3/3 successful trials per mode, and makes no efficiency claim.

Submission-owner actions: record this walkthrough, upload it, fill the public
demo/video URLs, and add deployment proof only if deployment is later approved.
These are intentionally outside the completed engineering scope.
